apiVersion: v1
kind: ConfigMap
metadata:
  name: x
data:
  config.ini: >
    # Instance name can be used to distinguish between different instances using
    backend api.getInstanceName()

    # host setting is relevant only for web deployments - set the host on which
    the server will listen

    host=0.0.0.0
